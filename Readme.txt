This Jar files will helps to you Record Screen Activities and convert them into video via Java Coding. 

How to use, 
* Import the two Jar files ScreenRecord.jar, jmf-2.1.1e.jar 
* To Start recording, Call ScreenRecorder.StartRecord(path where you want to store video); 
* To End recording, Call ScreenRecorder.EndRecord();